apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "minha-app.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "minha-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "minha-app.name" . }}
    spec:
      containers:
        - name: minha-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
